{% extends 'base.html.twig' %}
{% block title %}

{% endblock %}

{% block body %}
<div>
    <h3>Listes des utilisateurs inscrits</h3>
    <ul>
        {% for user in participants %}
        <li><a href="{{ path('admin_utilisateurs_detail',{'id':user.id}) }}">{{user.pseudo}}</a></li>
        {% endfor %}
    </ul>

</div>
{% endblock %}


