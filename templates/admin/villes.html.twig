{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}
<h1>Gérer les villes</h1>
    <div>
        <ul class="villes">
            {% for ville in villes %}
                <li>{{ ville.nom }}   {{ ville.codePostal }}  <a href="{{ path('admin_villes_suppr', {'id': ville.id}) }}">Supprimer</a></li>
            {% endfor %}

        </ul>
    </div>

    <div>
        <h3>Nouvelle ville</h3>
        {{ form_start(form) }}

        {{ form_row(form) }}
        <button>Ajouter</button>
        {{ form_end(form) }}

    </div>
{% endblock %}



