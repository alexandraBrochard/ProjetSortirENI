
<div class="navbar">
        <a href="{{ path('app_main') }}">Villes</a>
        <a href="{{ path('app_main') }}">Campus</a>
        <a href="{{ path('app_main') }}">Accueil</a>
        <a href="#">A propos</a>
            <div class="dropdown">
                <button class="dropbtn">Information
                    <i class="fa fa-caret-down"></i>
                </button>
                <div class="dropdown-content">
                    <a href="#">Contact</a>
                    <a href="#">A propos</a>
                </div>
            </div>
    {%  if app.user %}
        <a href="{{ path('app_main') }}">Mon profil</a>
        <a href="{{ path('app_logout') }}">Se déconnecter</a>
    {% else %}
        <a href="{{ path('app_register') }}">M'inscrire</a>
        <a href="{{ path('app_login') }}">Se connecter</a>
    {% endif %}

</div>

{# TODO: changer la route de mon profil + ville + Campus + Accueil + logo sortir #}
